# Scalar Gate (MLP) Configuration
# Inherits base hyperparameters for fair comparison

exp_name: scalar_gate_mlp
model_type: scalar_gate
modality: both
gate_type: mlp

# Base hyperparameters (SAME FOR ALL MODELS)
data_root: data/JU-LDD-task-b
save_dir: checkpoints
img_size: 224
patch_size: 16
num_layers: 4
embed_dim: 256
num_heads: 8
dropout: 0.1
batch_size: 4
num_epochs: 50
learning_rate: 0.0001
weight_decay: 0.01
num_workers: 2

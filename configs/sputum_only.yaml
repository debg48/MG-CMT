# Sputum-Only Baseline Configuration
# Inherits base hyperparameters for fair comparison

exp_name: sputum_only
model_type: unimodal
modality: sputum

# Base hyperparameters (SAME FOR ALL MODELS)
data_root: data/JU-LDD-task-b
save_dir: checkpoints
img_size: 224
patch_size: 16
num_layers: 4
embed_dim: 192
num_heads: 8
dropout: 0.3
batch_size: 4
num_epochs: 30
weight_decay: 0.15
num_workers: 2
